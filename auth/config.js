export const TOKEN_REQUEST_INTERVAL_SECONDS = 2
export const CODE_EXPIRY_SECONDS = 5 * 60
export const { BASE_URL, TABLE_NAME } = process.env
export const VERIFICATION_URI = `${BASE_URL}/auth/verify`
